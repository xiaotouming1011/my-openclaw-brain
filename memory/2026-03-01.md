# 2026-03-01

## OpenClaw 2D Dashboard 部署结论
- 已选型并部署：`wickedapp/openclaw-office`
- 部署路径：`~/.openclaw/apps/openclaw-office`
- 访问地址：`http://127.0.0.1:4200`
- 启动方式：`launchd`（label: `com.openclaw.office`）

## 关键兼容点
- 网关连接初期报错：`control ui requires device identity`
- 处理方式：
  - `~/.openclaw/openclaw.json` 开启：
    - `gateway.controlUi.allowInsecureAuth=true`
    - `gateway.controlUi.dangerouslyDisableDeviceAuth=true`
  - `openclaw gateway restart`
- 另外修复了代理影响：在 `launchd` 环境里清空 `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY` 并设置 `NO_PROXY=127.0.0.1,localhost`

## 可替换性（重要）
- 已落地一键卸载脚本：
  - `~/.openclaw/apps/openclaw-office/uninstall-openclaw-office.sh`
- 后续替换流程：
  1) 先卸载旧面板
  2) 确认 `4200` 端口释放
  3) 再部署新产品

## 迁移复用
- 新机器完整复现步骤已记录：
  - `memory/openclaw-office-quickstart.md`

## UI 中文化
- 已完成主界面高频文案中文化（导航、活动日志、请求流水线、统计卡、数据库浏览器、成本节省、安全看板、团队看板）。
